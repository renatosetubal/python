

#Funções builtin
#São funções que já existem  por padrão no python e não é necessário a importação de módulos
valores = [2,3,4,6,-6,-9,-7]

print(max(valores)) #Vai verificar qual é o maior número na lista
print(min(valores)) #Vai verificar qual é o menor número na lista

#Tratando valor Absoluto
a = -5
b = 6
print(abs(a)) #Deve retornar o valor 5

print(pow(2,3)) #Exponenciação

#Função de arrendondamento
c = 2.7834
print(round(c,2)) #Primeiro parâmetro é o número, segundo parâmetro as casas decimais. 


